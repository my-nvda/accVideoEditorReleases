package com.example.accessiblevideoeditor.ui

// This class has been intentionally removed.
// AppStringResources previously subclassed android.content.res.Resources using a deprecated
// constructor Resources(assets, displayMetrics, configuration) which is broken on Android 7+
// and causes crashes on Android 12+.
//
// String translation is now handled purely via AppStrings.get(context, resId) at the call site.
// No Resources subclassing or LayoutInflater reflection is needed or used.
